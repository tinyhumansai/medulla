# Program

Typed models shared by the public worker-roster and task-program endpoints.

## Contents

- [`mod.rs`](./mod.rs) — Typed models shared by the public worker-roster and task-program endpoints.
- [`types.rs`](./types.rs) — Wire models for the worker roster and operator-owned task program APIs.

## Maintenance

Keep this index synchronized when responsibilities move. Put shared data structures in `types.rs`, focused unit tests in `tests.rs` or a sibling `_tests.rs`, and preserve the module-level Rust documentation as the API source of truth.
