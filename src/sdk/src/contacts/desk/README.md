# Desk

`ContactDesk` — the book, the relay, and the clock bundled into the one handle a UI holds.

## Contents

- [`mod.rs`](./mod.rs) — `ContactDesk` — the book, the relay, and the clock bundled into the one handle a UI holds.
- [`types.rs`](./types.rs) — Data types for the `desk` module.

## Maintenance

Keep this index synchronized when responsibilities move. Put shared data structures in `types.rs`, focused unit tests in `tests.rs` or a sibling `_tests.rs`, and preserve the module-level Rust documentation as the API source of truth.
