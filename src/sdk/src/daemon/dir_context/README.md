# Dir Context

Workspace directory context for the capability probe.

## Contents

- [`mod.rs`](./mod.rs) — Workspace directory context for the capability probe.
- [`tests.rs`](./tests.rs) — Tests for the dir context module.
- [`types.rs`](./types.rs) — Data types for the `dir_context` module.

## Maintenance

Keep this index synchronized when responsibilities move. Put shared data structures in `types.rs`, focused unit tests in `tests.rs` or a sibling `_tests.rs`, and preserve the module-level Rust documentation as the API source of truth.
