# Transport

Encrypted Signal DM transport for the daemon.

## Contents

- [`mod.rs`](./mod.rs) — Encrypted Signal DM transport for the daemon.
- [`tests.rs`](./tests.rs) — Tests for the transport module.
- [`types.rs`](./types.rs) — Data types for the `transport` module.

## Maintenance

Keep this index synchronized when responsibilities move. Put shared data structures in `types.rs`, focused unit tests in `tests.rs` or a sibling `_tests.rs`, and preserve the module-level Rust documentation as the API source of truth.
