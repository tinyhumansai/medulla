# Probe

Building the `capabilities` payload the backend records for a worker.

## Contents

- [`mod.rs`](./mod.rs) — Building the `capabilities` payload the backend records for a worker.
- [`tests.rs`](./tests.rs) — Tests for the backend-shaped capability payload.

## Maintenance

Keep this index synchronized when responsibilities move. Put shared data structures in `types.rs`, focused unit tests in `tests.rs` or a sibling `_tests.rs`, and preserve the module-level Rust documentation as the API source of truth.
