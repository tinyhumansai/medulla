# Control

Owner-to-machine control frames for the harness session bus.

## Contents

- [`mod.rs`](./mod.rs) — Owner-to-machine control frames for the harness session bus.
- [`tests.rs`](./tests.rs) — Tests for the control module.
- [`types.rs`](./types.rs) — Data types for the `control` module.

## Maintenance

Keep this index synchronized when responsibilities move. Put shared data structures in `types.rs`, focused unit tests in `tests.rs` or a sibling `_tests.rs`, and preserve the module-level Rust documentation as the API source of truth.
