# Status

Derived session-status state machine for the SDK's v2 harness stream.

## Contents

- [`mod.rs`](./mod.rs) — Derived session-status state machine for the SDK's v2 harness stream.
- [`tests.rs`](./tests.rs) — Tests for the status module.
- [`types.rs`](./types.rs) — Data types for the `status` module.

## Maintenance

Keep this index synchronized when responsibilities move. Put shared data structures in `types.rs`, focused unit tests in `tests.rs` or a sibling `_tests.rs`, and preserve the module-level Rust documentation as the API source of truth.
