# Capabilities

Narrow capability interfaces over the compatibility-facing `Runtime`.

## Contents

- [`mod.rs`](./mod.rs) — Narrow capability interfaces over the compatibility-facing `Runtime`.
- [`tests.rs`](./tests.rs) — Contract tests for narrow runtime capability adapters.
- [`types.rs`](./types.rs) — Capability traits and their compatibility adapters.

## Maintenance

Keep this index synchronized when responsibilities move. Put shared data structures in `types.rs`, focused unit tests in `tests.rs` or a sibling `_tests.rs`, and preserve the module-level Rust documentation as the API source of truth.
