# Tests

Unit tests for the runtime trait surface's helper types — chiefly `WorkerOp` parsing and the snapshot defaults.

## Contents

- [`mod.rs`](./mod.rs) — Unit tests for the runtime trait surface's helper types — chiefly `WorkerOp` parsing and the snapshot defaults.
- [`types.rs`](./types.rs) — Test-only data types for the runtime contract.

## Maintenance

Keep this index synchronized when responsibilities move. Put shared data structures in `types.rs`, focused unit tests in `tests.rs` or a sibling `_tests.rs`, and preserve the module-level Rust documentation as the API source of truth.
