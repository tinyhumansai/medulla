# Completion

Turn-completion detection for a harness driven through its **interactive** interface.

## Contents

- [`mod.rs`](./mod.rs) — Turn-completion detection for a harness driven through its **interactive** interface.
- [`types.rs`](./types.rs) — Data types for the `completion` module.

## Maintenance

Keep this index synchronized when responsibilities move. Put shared data structures in `types.rs`, focused unit tests in `tests.rs` or a sibling `_tests.rs`, and preserve the module-level Rust documentation as the API source of truth.
