# Ops

`SessionOp` — the operator actions the Sessions screen dispatches, and the manager entry that applies one.

## Contents

- [`mod.rs`](./mod.rs) — `SessionOp` — the operator actions the Sessions screen dispatches, and the manager entry that applies one.
- [`types.rs`](./types.rs) — Data types for the `ops` module.

## Maintenance

Keep this index synchronized when responsibilities move. Put shared data structures in `types.rs`, focused unit tests in `tests.rs` or a sibling `_tests.rs`, and preserve the module-level Rust documentation as the API source of truth.
