# Routing

Session-class and transport routing: which lifetime a stimulus gets, and whether a provider can serve it interactively at all.

## Contents

- [`mod.rs`](./mod.rs) — Session-class and transport routing: which lifetime a stimulus gets, and whether a provider can serve it interactively at all.
- [`types.rs`](./types.rs) — Data types for the `routing` module.

## Maintenance

Keep this index synchronized when responsibilities move. Put shared data structures in `types.rs`, focused unit tests in `tests.rs` or a sibling `_tests.rs`, and preserve the module-level Rust documentation as the API source of truth.
