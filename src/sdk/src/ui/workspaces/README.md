# Workspaces

The workspaces surface: every directory the fleet can work in, as one list.

## Contents

- [`mod.rs`](./mod.rs) — The workspaces surface: every directory the fleet can work in, as one list.
- [`tests.rs`](./tests.rs) — Tests for the workspaces surface.
- [`types.rs`](./types.rs) — Data types for the workspaces surface.

## Maintenance

Keep this index synchronized when responsibilities move. Put shared data structures in `types.rs`, focused unit tests in `tests.rs` or a sibling `_tests.rs`, and preserve the module-level Rust documentation as the API source of truth.
