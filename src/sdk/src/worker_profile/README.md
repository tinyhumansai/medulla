# Worker Profile

The persisted first-run worker profile.

## Contents

- [`mod.rs`](./mod.rs) — The persisted first-run worker profile.
- [`tests.rs`](./tests.rs) — Tests for the worker profile module.
- [`types.rs`](./types.rs) — Data types for the `worker_profile` module.

## Maintenance

Keep this index synchronized when responsibilities move. Put shared data structures in `types.rs`, focused unit tests in `tests.rs` or a sibling `_tests.rs`, and preserve the module-level Rust documentation as the API source of truth.
