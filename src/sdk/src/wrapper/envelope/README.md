# Envelope

v2 session-envelope construction for the harness wrapper.

## Contents

- [`mod.rs`](./mod.rs) — v2 session-envelope construction for the harness wrapper.
- [`tests.rs`](./tests.rs) — Tests for the envelope module.
- [`types.rs`](./types.rs) — Data types for the `envelope` module.

## Maintenance

Keep this index synchronized when responsibilities move. Put shared data structures in `types.rs`, focused unit tests in `tests.rs` or a sibling `_tests.rs`, and preserve the module-level Rust documentation as the API source of truth.
