# Child

Spawning the harness child, and the uniform handle the run loop drives it through.

## Contents

- [`mod.rs`](./mod.rs) — Spawning the harness child, and the uniform handle the run loop drives it through.
- [`types.rs`](./types.rs) — Data types for the `child` module.

## Maintenance

Keep this index synchronized when responsibilities move. Put shared data structures in `types.rs`, focused unit tests in `tests.rs` or a sibling `_tests.rs`, and preserve the module-level Rust documentation as the API source of truth.
